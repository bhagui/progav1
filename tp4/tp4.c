#include <stdio.h>
#include <stdlib.h>


typedef struct element
{
int val;
struct element *nxt;
} element;

typedef element* llist;

Action ajout_tete(llist l, int val)
{
element* new = malloc(sizeof(element));
}

int main ()
{
llist liste1 = NULL;
}
